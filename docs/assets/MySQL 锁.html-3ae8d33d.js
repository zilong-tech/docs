import{_ as e,W as s,X as n,a1 as p}from"./framework-2afc6763.js";const o={},t=p(`<p><strong>锁详解</strong></p><p>锁是计算机协调多个进程或线程并发访问某一资源的机制。</p><p>在数据库中，除了传统的计算资源（如CPU、RAM、I/O等）的争用以外，数据也是一种供需要用户共享的资源。如何保证数据并发访问的一致性、有效性是所有数据库必须解决的一个问题，锁冲突也是影响数据库并发访问性能的一个重要因素。</p><p><strong>锁分类</strong></p><ul><li>从性能上分为乐观锁(用版本对比来实现)和悲观锁</li><li>从对数据库操作的类型分，分为读锁和写锁(都属于悲观锁)</li></ul><p>读锁（共享锁，S锁(<strong>S</strong>hared)）：针对同一份数据，多个读操作可以同时进行而不会互相影响</p><p>写锁（排它锁，X锁(e<strong>X</strong>clusive)）：当前写操作没有完成前，它会阻断其他写锁和读锁</p><ul><li>从对数据操作的粒度分，分为表锁和行锁</li></ul><p><strong>表锁</strong></p><p>每次操作锁住整张表。开销小，加锁快；不会出现死锁；锁定粒度大，发生锁冲突的概率最高，并发度最低；一般用在整表数据迁移的场景。</p><ul><li>手动增加表锁</li></ul><p>lock table 表名称 read(write),表名称2 read(write);</p><ul><li>查看表上加过的锁</li></ul><p>show open tables;</p><ul><li>删除表锁</li></ul><p>unlock tables;</p><p><strong>行锁</strong></p><p>每次操作锁住一行数据。开销大，加锁慢；会出现死锁；锁定粒度最小，发生锁冲突的概率最低，并发度最高。</p><p>InnoDB与MYISAM的最大不同有两点：</p><ul><li><strong>InnoDB支持事务（TRANSACTION）</strong></li><li><strong>InnoDB支持行级锁</strong></li></ul><p>InnoDB在执行查询语句SELECT时(非串行隔离级别)，不会加锁。但是update、insert、delete操作会加行锁。</p><p>简而言之，就是<strong>读锁会阻塞写，但是不会阻塞读。而写锁则会把读和写都阻塞</strong>。</p><p><strong>间隙锁(Gap Lock)</strong></p><p>间隙锁，锁的就是两个值之间的空隙。Mysql默认级别是repeatable-read，有办法解决幻读问题吗？间隙锁在某些情况下可以解决幻读问题。</p><p>假设account表里数据如下：</p><p><img src="http://img.xxfxpt.top/202112122210376.png" alt="image-20211212221026948"></p><p>那么间隙就有 id 为 (3,10)，(10,20)，(20,正无穷) 这三个区间，</p><p>在Session_1下面执行 update account set name = &#39;zhuge&#39; where id &gt; 8 and id &lt;18;，则其他Session没法在这个<strong>范围所包含的所有行记录(包括间隙行记录)以及行记录所在的间隙</strong>里插入或修改任何数据，即id在(3,20]区间都无法修改数据，注意最后那个20也是包含在内的。</p><p><strong>间隙锁是在可重复读隔离级别下才会生效。</strong></p><p>事务A：</p><p><img src="http://img.xxfxpt.top/202112122214934.png" alt="image-20211212221456600"></p><p>事务B：</p><p><img src="http://img.xxfxpt.top/202112122215601.png" alt="image-20211212221542369"></p><p>事务A：</p><p><img src="http://img.xxfxpt.top/202112122219782.png" alt="image-20211212221911685"></p><p>事务B：</p><p><img src="http://img.xxfxpt.top/202112122220278.png" alt="image-20211212222001149"></p><p><strong>临键锁(Next-key Locks)</strong></p><p>Next-Key Locks是行锁与间隙锁的组合。像上面那个例子里的这个(3,20]的整个区间可以叫做临键锁。</p><p><strong>无索引行锁会升级为表锁(RR级别会升级为表锁，RC级别不会升级为表锁)</strong></p><p>锁主要是加在索引上，如果对非索引字段更新，行锁可能会变表锁</p><p>session1 执行：update account set balance = 800 where name = &#39;lilei&#39;;</p><p>session2 对该表任一行操作都会阻塞住</p><p><strong>InnoDB的行锁是针对索引加的锁，不是针对记录加的锁。并且该索引不能失效，否则都会从行锁升级为表锁</strong>**。**</p><p>锁定某一行还可以用lock in share mode(共享锁) 和for update(排它锁)，例如：select * from test_innodb_lock where a = 2 for update; 这样其他session只能读这行数据，修改则会被阻塞，直到锁定行的session提交</p><p><strong>结论</strong></p><p>Innodb存储引擎由于实现了行级锁定，虽然在锁定机制的实现方面所带来的性能损耗可能比表级锁定会要更高一下，但是在整体并发处理能力方面要远远优于MYISAM的表级锁定的。当系统并发量高的时候，Innodb的整体性能和MYISAM相比就会有比较明显的优势了。</p><p>但是，Innodb的行级锁定同样也有其脆弱的一面，当我们使用不当的时候，可能会让Innodb的整体性能表现不仅不能比MYISAM高，甚至可能会更差。</p><p><strong>行锁分析</strong></p><p>通过检查InnoDB_row_lock状态变量来分析系统上的行锁的争夺情况</p><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>show status like &#39;innodb_row_lock%&#39;;              
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>对各个状态量的说明如下：</p><p>Innodb_row_lock_current_waits: 当前正在等待锁定的数量</p><p>Innodb_row_lock_time: 从系统启动到现在锁定总时间长度</p><p>Innodb_row_lock_time_avg: 每次等待所花平均时间</p><p>Innodb_row_lock_time_max：从系统启动到现在等待最长的一次所花时间</p><p>Innodb_row_lock_waits: 系统启动后到现在总共等待的次数</p><p>对于这5个状态变量，比较重要的主要是：</p><p>Innodb_row_lock_time_avg （等待平均时长）</p><p>Innodb_row_lock_waits （等待总次数）</p><p>Innodb_row_lock_time（等待总时长）</p><p>尤其是当等待次数很高，而且每次等待时长也不小的时候，我们就需要分析系统中为什么会有如此多的等待，然后根据分析结果着手制定优化计划。</p><p><strong>查看INFORMATION_SCHEMA系统库锁相关数据表</strong></p><p>​</p><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>-- 查看事务 
select * from INFORMATION_SCHEMA.INNODB_TRX; 
-- 查看锁 
select * from INFORMATION_SCHEMA.INNODB_LOCKS; 
-- 查看锁等待 
select * from INFORMATION_SCHEMA.INNODB_LOCK_WAITS;
-- 释放锁，trx_mysql_thread_id可以从INNODB_TRX表里查看到 
kill trx_mysql_thread_id 
-- 查看锁等待详细信息 
show engine innodb status\\G;       
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>​</p><p><strong>死锁</strong></p><p><strong>set tx_isolation=&#39;<strong>repeatable-read</strong>&#39;;</strong></p><p>Session_1执行：select * from account where id=1 for update;</p><p>Session_2执行：select * from account where id=2 for update;</p><p>Session_1执行：select * from account where id=2 for update;</p><p>Session_2执行：select * from account where id=1 for update;</p><p>查看近期死锁日志信息：show engine innodb status\\G;</p><p>大多数情况mysql可以自动检测死锁并回滚产生死锁的那个事务，但是有些情况mysql没法自动检测死锁</p><p><strong>锁优化建议</strong></p><ul><li>尽可能让所有数据检索都通过索引来完成，避免无索引行锁升级为表锁</li><li>合理设计索引，尽量缩小锁的范围</li><li>尽可能减少检索条件范围，避免间隙锁</li><li>尽量控制事务大小，减少锁定资源量和时间长度，涉及事务加锁的sql尽量放在事务最后执行</li><li>尽可能低级别事务隔离</li></ul>`,76),i=[t];function r(l,a){return s(),n("div",null,i)}const d=e(o,[["render",r],["__file","MySQL 锁.html.vue"]]);export{d as default};
